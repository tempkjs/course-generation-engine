# Dependencies
node_modules/

# Next.js build output
.next/
out/

# Env files (never commit real secrets)
.env
.env*.local
.env.example

# TypeScript incremental build info
*.tsbuildinfo

# Editor/OS
.DS_Store

# Test/coverage
coverage/

# Live-run validation output (ADR 0015) — durable on disk for a human to read, not committed
validation-output/
